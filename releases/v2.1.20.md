
# Release 2.1.20 (2026-08-05)

## Bug fixes

- **remove unused osv-scanner from C++ images (#504)**
osv-scanner was installed for the C++ AUDIT stage (#487), but that stage reverted to conan audit, leaving osv-scanner as unused cruft. Remove the prebuilt-binary install from common/cpp-analysis.dockerfile, the smoke presence probe, the auto-managed pins-registry entry (pins.yml, resolve_latest.py TOOL_REPOS/TOOL_ARGS, regenerated CATALOG.md), the pins tests that assert it, and the README toolset mentions.

Historical CHANGELOG/release notes are left intact. conan audit (the current, correct auditor, living in the tooling not the image) is untouched. The image purge rides the next release.
