
# Release 2.1.28 (2026-08-29)

## Chores

- **sync .gitignore to the canonical baseline (#582)**
Append the baseline lines this repo was missing so .gitignore is a verbatim superset of vergil_tooling.data/gitignore.baseline again. Existing local entries are preserved. Part of epic vergil-project/.github#322.

- **sync the vergil-managed .gitignore block (#584)**
Convert this repo's .gitignore to the composed vergil-managed block (base + primary language) — epic vergil-project/.github#325. Foreign-language ignore rules are removed; genuine repo-local entries are preserved outside the fence. Produced by the fleet migration sweep (task #2927).

- **re-add __pycache__/ as a repo-local ignore (#586)**
The .gitignore migration converted this repo to the base-only managed block, which drops the python fragment's __pycache__/ (this repo is not python-primary). But the Docker build tooling under docker/pins/ runs Python and writes bytecode, which then dirties develop (vrg-finalize-pr flagged docker/pins/__pycache__/ on PR #584). Add __pycache__/ back as a repo-local entry outside the managed fence. Part of epic vergil-project/.github#325.


## Features

- **add pytest-xdist to the Python dev image (#588) (#589)**
