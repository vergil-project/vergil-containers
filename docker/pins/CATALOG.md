# Pin catalog (generated — do not edit)

Generated by `docker/pins/generate_catalog.py` from the Dockerfiles + `pins.yml`. Run `docker/pins/generate_catalog.py` to refresh.

| tool | version | state | inducing release | deterministic | reason |
| --- | --- | --- | --- | --- | --- |
| actionlint | 1.7.12 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version checksums embed the version; the weekly bump-tools workflow delivers the FORWARD bump #260 asked for once upstream ships it. |
| git-cliff | 2.13.1 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version sha512 embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| go-test-coverage | 2.18.8 | active | 2.18.4 | yes | go-test-coverage >=2.18.4 requires Go >=1.26; fails on the Go 1.25 image. |
| go-test-coverage | 2.18.3 | active | 2.18.4 | yes | go-test-coverage >=2.18.4 requires Go >=1.26; fails on the Go 1.25 image. |
| hadolint | 2.14.0 | auto-managed | — | no | auto-managed (#435) — GitHub release binary + per-version sha256 embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| nfpm | 2.47.0 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version checksums embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| opentofu | 1.12.4 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version SHA256SUMS embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| scorecard | 5.5.0 | auto-managed | — | no | auto-managed (#435) — scorecard release tarball + scorecard_checksums.txt embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| shellcheck | 0.11.0 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball URL embeds the version; floated to the leading edge by the weekly bump-tools workflow. |
| shfmt | 3.13.1 | auto-managed | — | no | auto-managed (#435) — GitHub release binary URL embeds the version; floated to the leading edge by the weekly bump-tools workflow. |
| trivy | 0.72.0 | auto-managed | — | no | auto-managed (#435) — trivy release tarball + per-version checksums embed the version; floated to the leading edge by the weekly bump-tools workflow (weekly bumps also pick up CVE-fix releases, e.g. the 0.70.0 → 0.72.0 bump under #438). |
