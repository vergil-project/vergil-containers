# Pin catalog (generated — do not edit)

Generated by `docker/pins/generate_catalog.py` from the Dockerfiles + `pins.yml`. Run `docker/pins/generate_catalog.py` to refresh.

| tool | version | state | inducing release | deterministic | reason |
| --- | --- | --- | --- | --- | --- |
| actionlint | 1.7.12 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version checksums embed the version; the weekly bump-tools workflow delivers the FORWARD bump #260 asked for once upstream ships it. |
| git-cliff | 2.13.1 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version sha512 embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| go-test-coverage | 2.18.3 | active | 2.18.4 | yes | go-test-coverage >=2.18.4 requires Go >=1.26; fails on the Go 1.25 image. Only that branch is held — the Go 1.26 branch floated back to latest in #571, since nothing constrains it and a hand-written version there is drift, not policy. Retires with Go 1.25. |
| golangci-lint | 2.12.2 | active | 2.13.0 | yes | golangci-lint >=2.13.0 declares `go 1.26.0`; with GOTOOLCHAIN=local the Go 1.25 image cannot satisfy it and `go install @latest` fails the build. v2.12.2 is the last release carrying `go 1.25.0`. Go 1.26 stays unpinned. Retires with Go 1.25. |
| hadolint | 2.15.1 | auto-managed | — | no | auto-managed (#435) — GitHub release binary + per-version sha256 embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| nfpm | 2.47.0 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version checksums embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| opentofu | 1.12.6 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball + per-version SHA256SUMS embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| scorecard | 5.5.0 | auto-managed | — | no | auto-managed (#435) — scorecard release tarball + scorecard_checksums.txt embed the version; floated to the leading edge by the weekly bump-tools workflow. |
| shellcheck | 0.11.0 | auto-managed | — | no | auto-managed (#435) — GitHub release tarball URL embeds the version; floated to the leading edge by the weekly bump-tools workflow. |
| shfmt | 3.14.0 | auto-managed | — | no | auto-managed (#435) — GitHub release binary URL embeds the version; floated to the leading edge by the weekly bump-tools workflow. |
| trivy | 0.74.0 | auto-managed | — | no | auto-managed (#435) — trivy release tarball + per-version checksums embed the version; floated to the leading edge by the weekly bump-tools workflow (weekly bumps also pick up CVE-fix releases, e.g. the 0.70.0 → 0.72.0 bump under #438). |
